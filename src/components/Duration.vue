<script setup lang="ts">
</script>
<template>
    <div class="`flex items-center justify-center py-3 gap-6 bg-[var(--magnolia)]`">
            <span :class="`font-bold ${text-[var(--marine-blue)]}
                ${text-[var(--light-gray)]}`">
                Monthly
            </span>
            <input/>
            <span :class="`font-bold ${text-[var(--marine-blue)]} ${text-[var(--light-gray)]}`">
                Yearly
            </span>
        </div>
</template>